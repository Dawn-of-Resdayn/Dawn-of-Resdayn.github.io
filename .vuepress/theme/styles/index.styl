@require './palette.styl'

:root
  --mwRed $mwRed
  --textColor $textColor
  --darkColor $darkColor

html
body
.sidebar
  color var(--textColor)
  background-color var(--darkColor)

.navbar
  background-color var(--mwRed)
